---
import BaseLayout from "../layouts/BaseLayout.astro";
import ResponsiveImage from "../components/ResponsiveImage.astro";
import { getEntry } from "astro:content";

type TeamMember = {
  eyebrow: string;
  name: string;
  image: string;
  imageAlt: string;
  paragraphs: string[];
  email: string;
  phone: string;
  linkedin: string;
  credentialsEyebrow: string;
  credentials: string;
};

type AboutData = {
  eyebrow: string;
  title: string;
  titleAccent: string;
  lede: string;
  firm: { eyebrow: string; title: string; text: string };
  team: TeamMember[];
};

const about = await getEntry("pages", "about");
if (!about || !about.data.firm || !about.data.team) {
  throw new Error("About page content is missing.");
}

const aboutData = about.data as AboutData;
const credentialItems = (credentials: string) => credentials.split(" · ").map((credential) => credential.trim()).filter(Boolean);
---

<BaseLayout
  title="About | Mercier Talent Solutions"
  description={aboutData.lede}
  image="/images/optimized/julia-mercier-headshot-1400.jpg"
>
  <section class="section about-hero">
    <div class="container about-hero-inner">
      <div class="about-hero-copy">
        <p class="eyebrow">{aboutData.eyebrow}</p>
        <h1>{aboutData.title} <em>{aboutData.titleAccent}</em></h1>
        <p>{aboutData.lede}</p>
      </div>
      <div class="about-rings" aria-hidden="true">
        <span></span>
        <span></span>
      </div>
    </div>
  </section>

  <section class="section firm-band">
    <div class="container">
      <div class="firm-card">
        <div class="firm-kicker">
          <p class="eyebrow">{aboutData.firm.eyebrow}</p>
          <h2>{aboutData.firm.title}</h2>
        </div>
        <p class="firm-text">{aboutData.firm.text}</p>
      </div>
    </div>
  </section>

  <section class="section team-band">
    <div class="container team-list">
      {aboutData.team.map((person, index) => (
        <article class={`team-card person-${index + 1}`}>
          <div class="media-frame portrait">
            <ResponsiveImage src={person.image} alt={person.imageAlt} sizes="(min-width: 900px) 38vw, 100vw" />
          </div>
          <div class="team-copy">
            <p class="eyebrow">{person.eyebrow}</p>
            <h2>{person.name}</h2>
            {person.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
            <div class="meta-row">
              <a href={`mailto:${person.email}`}>{person.email}</a>
              <a href={`tel:${person.phone.replaceAll(/[^\d+]/g, "")}`}>{person.phone}</a>
              <a href={person.linkedin}>LinkedIn</a>
            </div>
            <div class="credentials">
              <p class="eyebrow">{person.credentialsEyebrow}</p>
              <div class="credential-list">
                {credentialItems(person.credentials).map((credential) => <span>{credential}</span>)}
              </div>
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .about-hero {
    position: relative;
    --about-scroll: 0;
    min-height: clamp(500px, 55vw, 620px);
    overflow: hidden;
    padding: 0;
    background: var(--color-blue-dark);
    color: var(--color-white);
  }

  .about-hero-inner {
    position: relative;
    display: grid;
    min-height: clamp(420px, 48vw, 520px);
    align-items: center;
  }

  .about-hero-copy {
    max-width: var(--reading-width);
    padding-top: clamp(1.25rem, 4vw, 3.75rem);
    translate: 0 calc(var(--about-scroll) * 16px);
    transition: translate 80ms linear;
    will-change: translate;
  }

  .about-hero .eyebrow {
    color: rgba(255, 255, 255, 0.72);
  }

  .about-hero .eyebrow::before {
    background: rgba(255, 255, 255, 0.62);
  }

  .about-rings {
    position: absolute;
    right: -200px;
    bottom: -280px;
    width: 720px;
    height: 520px;
    pointer-events: none;
    translate: calc(var(--about-scroll) * -24px) calc(var(--about-scroll) * -16px);
    transition: translate 80ms linear;
    will-change: translate;
  }

  .about-rings span {
    position: absolute;
    border: 1px solid rgba(233, 231, 226, 0.22);
    border-radius: 50%;
  }

  .about-rings span:first-child {
    inset: 20px 70px 70px 20px;
  }

  .about-rings span:last-child {
    inset: 130px 0 0 170px;
  }

  .firm-band {
    background: var(--color-white);
    padding: clamp(3.8rem, 5.4vw, 5.15rem) 0;
  }

  .team-card {
    display: grid;
    grid-template-columns: minmax(240px, 340px) minmax(0, 960px);
    gap: clamp(3.35rem, 5.6vw, 5.75rem);
    align-items: start;
    justify-content: center;
    max-width: 1180px;
    margin-inline: auto;
  }

  .firm-card {
    display: grid;
    --firm-scroll: 0;
    grid-template-columns: minmax(280px, 0.46fr) minmax(360px, 0.54fr);
    gap: clamp(3rem, 5vw, 4.85rem);
    align-items: center;
    max-width: 1040px;
    min-height: clamp(190px, 17vw, 230px);
    margin-inline: auto;
  }

  .firm-kicker {
    display: grid;
    align-content: center;
    max-width: 430px;
    min-height: 100%;
  }

  .firm-text {
    max-width: 560px;
    margin: 0;
    line-height: 1.55;
  }

  h1 {
    margin: 2.35rem 0 1.55rem;
    max-width: var(--reading-width);
    color: var(--color-white);
    font-family: var(--font-heading);
    font-size: var(--page-title-size);
    font-weight: 700;
    line-height: 1;
  }

  .about-hero h1 em {
    color: var(--color-blue);
  }

  p:not(.eyebrow) {
    color: var(--color-ink);
    font-size: var(--paragraph-size);
    line-height: 1.6;
  }

  .about-hero p:not(.eyebrow) {
    max-width: 860px;
    color: rgba(255, 255, 255, 0.78);
    font-size: var(--paragraph-size);
    line-height: 1.55;
  }

  h2 {
    margin: 0.45rem 0 var(--heading-content-gap);
    color: var(--color-ink);
    font-family: var(--font-heading);
    font-size: clamp(1.75rem, 3.5vw, 3rem);
    font-weight: 700;
    line-height: 1.12;
  }

  .team-band {
    background: var(--color-paper);
    padding: clamp(5.25rem, 7.2vw, 7rem) 0 clamp(6.3rem, 8.5vw, 8.4rem);
  }

  .team-list {
    display: grid;
    gap: clamp(5.5rem, 7.4vw, 7.4rem);
  }

  .firm-card .eyebrow {
    letter-spacing: 6px;
  }

  .firm-card .eyebrow::before {
    background: var(--color-blue);
  }

  .firm-card .eyebrow + h2 {
    margin-top: var(--heading-label-gap);
  }

  .firm-card h2 {
    max-width: 430px;
    font-size: clamp(1.9rem, 2.2vw, 2.15rem);
    line-height: 1.14;
  }

  .team-copy > .eyebrow {
    letter-spacing: 6px;
  }

  .team-copy > .eyebrow::before,
  .credentials .eyebrow::before {
    content: none;
  }

  .team-copy > .eyebrow + h2 {
    margin-top: var(--heading-label-gap);
  }

  .team-copy > h2 {
    margin-bottom: var(--heading-content-gap);
    font-size: clamp(2.2rem, 3.6vw, 3.05rem);
    line-height: 1.08;
  }

  .team-copy > p:not(.eyebrow) {
    max-width: 880px;
    margin: 0 0 1.35rem;
    line-height: 1.58;
  }

  .team-copy > p:not(.eyebrow):last-of-type {
    margin-bottom: 1.85rem;
  }

  .team-copy .meta-row {
    margin-top: 0.1rem;
    margin-bottom: 2.35rem;
  }

  .portrait {
    width: min(100%, 340px);
    aspect-ratio: 1729 / 1764;
    justify-self: start;
    border-radius: 0;
    box-shadow: 0 24px 70px rgba(23, 37, 62, 0.1);
  }

  .person-1 .portrait {
    aspect-ratio: 1111 / 1416;
  }

  .person-2 .portrait {
    aspect-ratio: 1111 / 1416;
  }

  .portrait :global(img) {
    object-position: center;
  }

  .person-1 .portrait :global(img) {
    object-position: center 34%;
  }

  .firm-card[data-reveal],
  .team-card[data-reveal] {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 900ms var(--ease-soft), transform 900ms var(--ease-soft);
  }

  .firm-card[data-reveal].is-visible,
  .team-card[data-reveal].is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .firm-card[data-reveal] .firm-kicker,
  .firm-card[data-reveal] .firm-text,
  .team-card[data-reveal] .portrait,
  .team-card[data-reveal] .team-copy {
    opacity: 0;
    transform: translateY(18px);
    transition: opacity 860ms var(--ease-soft), transform 860ms var(--ease-soft);
  }

  .firm-card[data-reveal] .firm-text,
  .team-card[data-reveal] .team-copy {
    transition-delay: 120ms;
  }

  .firm-card[data-reveal].is-visible .firm-kicker,
  .firm-card[data-reveal].is-visible .firm-text,
  .team-card[data-reveal].is-visible .portrait,
  .team-card[data-reveal].is-visible .team-copy {
    opacity: 1;
    transform: translateY(0);
  }

  .firm-card[data-reveal].is-visible .firm-kicker {
    transform: translate3d(0, calc(var(--firm-scroll) * -8px), 0);
  }

  .firm-card[data-reveal].is-visible .firm-text {
    transform: translate3d(0, calc(var(--firm-scroll) * 6px), 0);
  }

  .credentials {
    margin-top: 2rem;
    border-top: 1px solid var(--color-line);
    padding-top: 1.5rem;
  }

  .credential-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 0.8rem;
    margin-top: 1.45rem;
  }

  .credential-list span {
    border: 1px solid var(--color-line);
    padding: 0.65rem 1rem;
    background: var(--color-white);
    color: var(--color-ink);
    font-size: 0.92rem;
    line-height: 1.25;
  }

  a {
    color: var(--color-blue-dark);
  }

  @media (max-width: 820px) {
    .firm-card,
    .team-card {
      grid-template-columns: 1fr;
    }

    .firm-card,
    .firm-kicker,
    .firm-text,
    .team-copy > p:not(.eyebrow) {
      max-width: 100%;
    }

    .about-hero {
      min-height: 520px;
    }

    .firm-band {
      padding: clamp(3.75rem, 11vw, 5rem) 0;
    }

    .team-card {
      gap: 2.25rem;
    }

    .portrait {
      width: min(100%, 320px);
    }
  }

  .about-hero-copy {
    animation: aboutHeroIn 900ms var(--ease-soft) both;
  }

  .about-rings {
    animation: ringsFloat 12s ease-in-out infinite;
  }

  .about-rings span {
    animation: ringsBreathe 8s ease-in-out infinite;
  }

  .about-rings span:last-child {
    animation-delay: 1.3s;
  }

  @media (prefers-reduced-motion: reduce) {
    .about-hero-copy,
    .about-rings,
    .about-rings span {
      animation: none;
      translate: none;
    }

    .firm-card[data-reveal],
    .team-card[data-reveal],
    .firm-card[data-reveal] .firm-kicker,
    .firm-card[data-reveal] .firm-text,
    .team-card[data-reveal] .portrait,
    .team-card[data-reveal] .team-copy {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }

  @keyframes aboutHeroIn {
    from {
      opacity: 0;
      transform: translateY(18px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes ringsFloat {
    0%,
    100% {
      transform: translate3d(0, 0, 0);
    }

    50% {
      transform: translate3d(-18px, -12px, 0);
    }
  }

  @keyframes ringsBreathe {
    0%,
    100% {
      opacity: 0.75;
      transform: scale(1);
    }

    50% {
      opacity: 1;
      transform: scale(1.025);
    }
  }
</style>

<script>
  const aboutMotionQuery = window.matchMedia("(prefers-reduced-motion: reduce)");
  const aboutHero = document.querySelector<HTMLElement>(".about-hero");
  const firmCard = document.querySelector<HTMLElement>(".firm-card");

  if (!aboutMotionQuery.matches) {
    let aboutTicking = false;

    const updateAboutDepth = () => {
      if (aboutHero) {
        const rect = aboutHero.getBoundingClientRect();
        const progress = Math.min(Math.max(-rect.top / Math.max(rect.height, 1), 0), 1);
        aboutHero.style.setProperty("--about-scroll", progress.toFixed(3));
      }

      if (firmCard) {
        const rect = firmCard.getBoundingClientRect();
        const midpoint = window.innerHeight * 0.62;
        const progress = Math.min(Math.max((midpoint - rect.top) / Math.max(rect.height, 1), -1), 1);
        firmCard.style.setProperty("--firm-scroll", progress.toFixed(3));
      }

      aboutTicking = false;
    };

    const requestAboutDepthUpdate = () => {
      if (aboutTicking) {
        return;
      }

      aboutTicking = true;
      requestAnimationFrame(updateAboutDepth);
    };

    updateAboutDepth();
    window.addEventListener("scroll", requestAboutDepthUpdate, { passive: true });
    window.addEventListener("resize", requestAboutDepthUpdate);
  }
</script>
